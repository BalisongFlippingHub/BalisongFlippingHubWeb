import { Post } from "../modals/Post";

interface params {
    postObj: Post
}

const ProfilePost = ({ postObj }: params) => {

    return (
        <div>
            
        </div>
    )
}

export default ProfilePost;