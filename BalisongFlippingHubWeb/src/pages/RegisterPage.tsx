import UserRegistrationForm from "../components/UserRegistrationForm";

const RegisterPage = () => {
  return (
    <section className="w-full h-screen lg:pl-[192px] pt-[64px] flex justify-center items-center">
      <UserRegistrationForm />
    </section>
  );
};

export default RegisterPage;
