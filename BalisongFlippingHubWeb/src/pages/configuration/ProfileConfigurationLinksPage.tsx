interface params {
  linkType: string;
}

const ProfileConfigurationLinksPage = ({ linkType }: params) => {
  return (
    <section>
      <h5>Links</h5>
    </section>
  );
};

export default ProfileConfigurationLinksPage;
